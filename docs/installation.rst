============
Installation
============

At the command line::

    $ easy_install django-social-photostream

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-social-photostream
    $ pip install django-social-photostream
